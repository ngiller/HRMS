// SPA mode: semua halaman di-render client-side via fallback index.html
// adapter-static akan serve index.html untuk semua route yang tidak di-prerender
export const ssr = false;
export const prerender = false;
